*** Targets ***

probe = FPing

menu = Top
title = Network Latency
remark = Stock target set plus custom layers (LAN, ISP, router DNS) \
         appended at the end — trim what you don't need.

+ InternetSites

menu = Internet Sites
title = Internet Sites

++ Facebook
menu = Facebook
title = Facebook
host = facebook.com

++ Youtube
menu = YouTube
title = YouTube
host = youtube.com

++ JupiterBroadcasting
menu = JupiterBroadcasting
title = JupiterBroadcasting
host = jupiterbroadcasting.com

++ GoogleSearch
menu = Google
title = google.com
host = google.com

++ GoogleSearchIpv6
menu = Google
probe = FPing6
title = ipv6.google.com
host = ipv6.google.com

++ linuxserverio
menu = linuxserver.io
title = linuxserver.io
host = linuxserver.io

+ Europe

menu = Europe
title = European Connectivity

++ Germany

menu = Germany
title = The Fatherland

+++ TelefonicaDE

menu = Telefonica DE
title = Telefonica DE
host = www.telefonica.de

++ Switzerland

menu = Switzerland
title = Switzerland

+++ CernIXP

menu = CernIXP
title = Cern Internet eXchange Point
host = cixp.web.cern.ch

+++ SBB

menu = SBB
title = SBB
host = www.sbb.ch

++ UK

menu = United Kingdom
title = United Kingdom

+++ CambridgeUni

menu = Cambridge
title = Cambridge
host = cam.ac.uk

+++ UEA

menu = UEA
title = UEA
host = uea.ac.uk

+ USA

menu = North America
title = North American Connectivity

++ MIT

menu = MIT
title = Massachusetts Institute of Technology Webserver
host = web.mit.edu

++ UCB

menu = U. C. Berkeley
title = U. C. Berkeley Webserver
host = www.berkeley.edu

++ UCSD

menu = U. C. San Diego
title = U. C. San Diego Webserver
host = ucsd.edu

++ UMN

menu =  University of Minnesota
title = University of Minnesota
host = twin-cities.umn.edu

++ OSUOSL

menu = Oregon State University Open Source Lab
title = Oregon State University Open Source Lab
host = osuosl.org

+ DNS
menu = DNS
title = DNS

++ GoogleDNS1
menu = Google DNS 1
title = Google DNS 8.8.8.8
host = 8.8.8.8

++ GoogleDNS2
menu = Google DNS 2
title = Google DNS 8.8.4.4
host = 8.8.4.4

++ OpenDNS1
menu = OpenDNS1
title = OpenDNS1
host = 208.67.222.222

++ OpenDNS2
menu = OpenDNS2
title = OpenDNS2
host = 208.67.220.220

++ CloudflareDNS1
menu = Cloudflare DNS 1
title = Cloudflare DNS 1.1.1.1
host = 1.1.1.1

++ CloudflareDNS2
menu = Cloudflare DNS 2
title = Cloudflare DNS 1.0.0.1
host = 1.0.0.1

++ L3-1
menu = Level3 DNS 1
title = Level3 DNS 4.2.2.1
host = 4.2.2.1

++ L3-2
menu = Level3 DNS 2
title = Level3 DNS 4.2.2.2
host = 4.2.2.2

++ Quad9
menu = Quad9
title = Quad9 DNS 9.9.9.9
host = 9.9.9.9

+ DNSProbes
menu = DNS Probes
title = DNS Probes
probe = DNS

++ GoogleDNS1
menu = Google DNS 1
title = Google DNS 8.8.8.8
host = 8.8.8.8

++ GoogleDNS2
menu = Google DNS 2
title = Google DNS 8.8.4.4
host = 8.8.4.4

++ OpenDNS1
menu = OpenDNS1
title = OpenDNS1
host = 208.67.222.222

++ OpenDNS2
menu = OpenDNS2
title = OpenDNS2
host = 208.67.220.220

++ CloudflareDNS1
menu = Cloudflare DNS 1
title = Cloudflare DNS 1.1.1.1
host = 1.1.1.1

++ CloudflareDNS2
menu = Cloudflare DNS 2
title = Cloudflare DNS 1.0.0.1
host = 1.0.0.1

++ L3-1
menu = Level3 DNS 1
title = Level3 DNS 4.2.2.1
host = 4.2.2.1

++ L3-2
menu = Level3 DNS 2
title = Level3 DNS 4.2.2.2
host = 4.2.2.2

++ Quad9
menu = Quad9
title = Quad9 DNS 9.9.9.9
host = 9.9.9.9

++ Router
menu = Router DNS
title = DNS query latency via router (192.168.1.1)
host = 192.168.1.1

+ LAN

menu = LAN
title = Local Network

++ Router
menu = Router
title = Router / Gateway (192.168.1.1)
host = 192.168.1.1

++ pve1
menu = pve-1
title = Proxmox host pve-1 (192.168.1.205)
host = 192.168.1.205

++ Plex
menu = Plex
title = Plex LXC (192.168.1.176)
host = 192.168.1.176

+ ISP

menu = ISP
title = ISP

++ FirstHop
menu = First Hop
# NOTE: this target is snapshotted to a PUBLIC png (see smokeping-graph-snapshot in
# smokeping.yml). smokeping renders `title` into the image, so the title must not
# contain an address. any target added to the snapshot list needs the same treatment —
# this is a security property of this file now, not a cosmetic choice. `host` is safe:
# it is never drawn into the graph.
title = ISP first hop past the router
host = 72.8.112.2

+ Tailnet

menu = Tailnet
title = Tailscale Funnel Response Time
remark = End-to-end https response time through the tailscale funnel (public path). \
         A single node degrading here while its siblings stay flat means that node's \
         DERP connection has gone bad — restart its ts-* sidecar, then the app. \
         Healthy is ~0.3s; the July 2026 ts-homepage incident sat at 5-30s. \
         /favicon.ico 404s on homepage and calibre — that is fine and deliberate: \
         this probe times the tls+transport path, and a small error page exercises \
         it without pulling the 40KB dashboard five times per cycle.
probe = Curl
# `-s`: silent. Without this flag, every probe writes a progress meter to
# stderr — ~78% of `docker logs smokeping` and ~1.9MB/day into an uncapped json-file log.
# `--dns-servers` forces the ping to resolve through the public internet
# route. Otherwise it can silently fall back to resolving locally.
# `-4` pins IPv4 to keep the series continuous with the pre-fix history.
extraargs = -s -4 --dns-servers 1.1.1.1,8.8.8.8

++ HomepageFunnel
menu = Homepage
title = Homepage via funnel
host = homepage.tail17ddbe.ts.net
urlformat = https://%host%/favicon.ico

++ KomgaFunnel
menu = Komga
title = Komga via funnel
host = komga.tail17ddbe.ts.net
urlformat = https://%host%/favicon.ico

++ CalibreFunnel
menu = Calibre-Web
title = Calibre-Web-Automated via funnel
host = calibre.tail17ddbe.ts.net
urlformat = https://%host%/favicon.ico

# multihost target: overlays the three graphs above into one chart rather than
# probing anything itself, so it costs no extra requests. this is the view worth
# looking at — one line diverging while the others stay flat is the whole signal.
++ AllFunnels
menu = All Funnels
title = All funnels compared
host = /Tailnet/HomepageFunnel /Tailnet/KomgaFunnel /Tailnet/CalibreFunnel
